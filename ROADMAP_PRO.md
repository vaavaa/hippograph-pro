# HippoGraph Pro — Roadmap

**Repository:** github.com/artemMprokhorov/hippograph-pro
**Philosophy:** Add capabilities, don't rewrite foundation. Zero LLM cost as core advantage.
**Last Updated:** February 28, 2026

---

## Phase 1 — Quick Wins ✅ COMPLETED
*Быстрые улучшения поиска и визуализации без больших архитектурных изменений.*

### 1. RRF Fusion ✅
*Альтернативный способ объединения результатов поиска. Вместо ручной настройки весов (семантика + граф + BM25) математически правильно смешивает ранжи.рунги.*
- [x] Реализован (src/rrf_fusion.py)
- [x] A/B тест: RRF vs blend — оба 100% P@5 на regression suite
- [x] Переключается через `FUSION_METHOD=blend|rrf`

### 2. Graph Viewer ✅
*Визуальное отображение графа памяти. Помогает видеть как идеи связаны между собой, какие темы центральные.*
- [x] Цветовая подсветка кластеров (community highlighting)
- [x] Размер ноды = важность (PageRank-based sizing)
- [ ] Подписи кластеров (deferred)

---

## Phase 2 — Entity Extraction & Benchmarking ✅ COMPLETED
*Умное распознавание сущностей (людей, мест, технологий) в записях + измерение качества поиска на стандартном benchmark.*

### 3. GLiNER NER ✅
*Модель для распознавания сущностей (Named Entity Recognition). Работает без LLM — понимает любые типы сущностей без предварительной настройки.*
- [x] GLiNER client (src/gliner_client.py)
- [x] Распознаёт наши типы: person, org, location, tech, concept и др.
- [x] Бенчмарк: 257ms среднее, 35x быстрее Ollama, качество LLM-уровня
- [x] Цепочка extraction: GLiNER → spaCy → regex

### 4. Ollama ❌ Удалён (commit 78779d0)
*Ollama — локальный LLM сервер, который первоначально использовался для extraction. GLiNER оказался в 35 раз быстрее и стабильнее. Удаление освободило ~13GB.*
- [x] Удалён из docker-compose.yml
- [x] ollama_client.py удалён (207 строк)

### 5. LOCOMO Benchmark ✅ — 66.8% Recall@5
*LOCOMO — стандартный публичный benchmark для систем памяти. Recall@5 = в пяти лучших результатах поиска есть правильный ответ.*
- [x] Turn-level: 44.2% Recall@5
- [x] Hybrid granularity (3-turn chunks): +21.3%
- [x] Cross-encoder reranking (переранжирование результатов для точности)
- [x] Bi-temporal model (извлечение времени события из текста)
- [x] Query decomposition (очистка запроса от временных маркеров)

### 6. License Audit ✅
*Проверка что все библиотеки допускают коммерческое использование без ограничений.*
- [x] Все компоненты проверены (MIT, Apache 2.0, BSD-3)
- [x] THIRD_PARTY_LICENSES.md добавлен в репо
- [x] GLiNER v2.1+ (Apache 2.0) — ок; v1/base (CC BY-NC 4.0) — НЕ используется
- [x] GLiNER2 (Apache 2.0) — ок

---

## Phase 2.5 — Sleep-Time Compute, Infrastructure & Memory 🔄 IN PROGRESS
*Фоновая обработка графа пока Claude не работает, защита важных воспоминаний, инфраструктура.*

### 7. GLiNER2 Relation Extraction ✅
*GLiNER2 — большая модель (205M параметров) которая распознаёт не просто сущности, а связи между ними: кто где работает, что использует, что чем является. Создаёт типизированные связи в графе.*
- [x] GLiNER2 добавлен в Docker контейнер
- [x] spaCy typed relations (step_spacy_relations) — весь граф, каждый sleep cycle
- [x] GLiNER2 инкрементально — только новые ноды, batch_size=5, без OOM
- [x] Conflict resolution — если связь уже есть, новая не перезаписывает её — сохраняется в истории

### 8. Sleep-Wake цикл 🔄
*Аналог биологического сна. Пока система не работает, она проводит фоновое обслуживание: обновляет веса связей, ищет дубликаты, пересчитывает важность нод.*

**Light Sleep** ✅ (быстрый, каждые ~50 новых нот):
- [x] Stale edge decay — слабые связи постепенно затухают (важные защищены)
- [x] Anchor boost — ключевые ноты автоматически получают статус critical
- [x] Duplicate scan — поиск почти одинаковых нот
- [x] PageRank — пересчёт важности каждой ноды в графе
- [x] Автозапуск по порогу (sleep_scheduler)
- [x] Snapshot до каждого запуска — если что-то пошло не так, можно откатиться

**Deep Sleep** 🔄 (тяжёлый, ежедневно):
- [x] GLiNER2 relation extraction (инкрементально)
- [x] spaCy typed relations (весь граф)
- [ ] Cluster consolidation — объединение тематически близких нот в кластеры
- [ ] Extractive summaries — автоматические краткие описания кластеров (TF-IDF + PageRank лучшая нота)
- [ ] Contradiction detection — поиск противоречащих воспоминаний

**REM Sleep** (эксперимент, Phase 3):
- [ ] Случайные проходы по графу (с TrueRNG аппаратным источником случайности)
- [ ] Неожиданные ассоциации — система «видит сны», находит неочевидные связи

### 9. Anchor Memory ✅
*Некоторые воспоминания не должны затухать никогда — ключевые события, история проекта, безопасность. Категории с разной скоростью затухания.*
- [x] anchor — не затухает вообще
- [x] self-reflection, milestone, security — затухают очень медленно
- [x] Связи к защищённым нодам тоже не затухают
- [x] 67 существующих нот переведены в critical при первом запуске

### 10. Studio MCP ✅
*Инструмент который позволяет Claude работать напрямую с файлами и командами на Mac Studio через Claude.ai — в том числе с мобильного телефона.*
- [x] Единый ngrok туннель для hippograph + studio-mcp
- [x] 6 инструментов: exec, read, write, list, delete, health
- [x] Whitelist команд — нельзя запустить что попало
- [x] SSH для git push из контейнера
- [x] ARCHITECTURE.md — процедура rebuild

### 11. End-to-End QA Benchmark ✅ — F1=38.7%
*Проверка не просто нашёл ли поиск правильный документ, а дал ли правильный ответ через LLM. GPT-4 без памяти = 32.1%, у нас 38.7% (+6.6pp).*
- [x] Pipeline: retrieval + Claude Haiku + F1/ROUGE скоринг
- [x] 1,311 QA пар из 651 персональной ноты

### 12. Skills Ingestion 🔄
*Навыки ингестируются как опыт с эмоциональным весом, а не просто файлы которые читает система. Подобно тому как человек усваивает навык через практику.*
- [ ] huggingface/skills (2.1K stars)
- [ ] get-shit-done (12.8K stars)
- [ ] BowTiedSwan/rlm-skill (ArXiv:2512.24601)
- [ ] SkillRL (ArXiv:2602.08234)

### 13. Docker Cleanup
*Остатки старых образов занимают ~70GB на диске.*
- [x] Удалены semantic-memory-v2 (~12GB освобождено)
- [ ] Очистка остальных старых образов + build cache (~70GB)

---

## Phase 3 — Research & Hardening
*Научные эксперименты, безопасность, масштабирование.*

### 14. Benchmark Reproducibility — MEDIUM
*Сейчас никто не может повторить наши 66.8% — нет фиксированного seed и публичного мини-датасета. Это ослабляет доверие к цифрам.*
- [ ] Фиксированный seed в locomo_adapter.py
- [ ] Анонимизированный мини-датасет (10 QA пар без приватных данных)
- [ ] Инструкция «как воспроизвести результат» (Docker + dataset + команды)

### 15. LLM Temporal Reasoning
*Темпоральные вопросы («что было до/после X») — самое слабое место сейчас (36.5%). Метод TReMu (научная работа, ACL 2025) поднимает до 77.67% через генерацию кода.*
- [ ] Детекция временных запросов → генерация кода → выполнение → фильтрация
- [ ] Timeline при ингестировании

### 16. Entity Resolution
*Сейчас «Apple» в разных записях может быть компанией или фруктом. Система не различает. Entity Resolution — объединение одинаковых по смыслу сущностей.*
- [ ] Различение по контексту (Apple компания vs фрукт)
- [ ] Объединение синонимов (ML → Machine Learning)
- [ ] Coreference — «он», «она», «они» → конкретная сущность

### 17. Hierarchical Tree Index
*Деревовидный индекс поверх графа. Сначала находим нужный кластер, потом точный поиск внутри. Ускоряет поиск на больших графах.*
- [ ] Дерево из NetworkX communities + subcommunities
- [ ] Гибрид: spreading activation + tree search

### 18. Multi-Agent Architecture
*Второй AI агент со своей памятью, обмен между агентами, эксперименты с сознанием.*
- [ ] Второй агент с отдельным пространством памяти
- [ ] TrueRNG — аппаратный источник настоящей случайности для REM сна
- [ ] Протокол обмена памятью между агентами
- [ ] Фреймворк для экспериментов с сознанием

### 19. User-Defined Anchor Policies — MEDIUM
*Сейчас правила защиты памяти прописаны в коде. Здесь — возможность задавать свои правила через MCP без редактирования кода.*
- [ ] Таблица anchor_policies в SQLite
- [ ] MCP tools: add/list/remove anchor policy
- [ ] sleep_compute читает политики + объединяет с хардкодными

### 20. Optional Edge Pruning — LOW (user opt-in only)
*Удаление слабых связей — только по явному запросу. Слабая связь может быть редкой но критической ассоциацией. Система не знает что важно — только пользователь.*
- [ ] `preview_prunable_edges(threshold)` — показать что будет удалено, БЕЗ удаления
- [ ] `prune_edges(threshold, confirm=True)` — требует явное подтверждение
- [ ] Защищённые категории всегда exempt
- [ ] Snapshot до любого pruning
- **НИКОГДА:** автоматически, без preview, защищённые связи

### 21. Spreading Activation Scalability — MEDIUM
*Спрединг активация — алгоритм поиска по ассоциациям — замедляется с ростом графа. Варианты решения:*
- [ ] **A: Subgraph Sampling** — SA только по локальному subgraph ANN-кандидатов. Минимально кода.
- [ ] **B: Community-Aware Routing** — SA только внутри релевантных сообществ + 1 шаг наружу.
- [ ] **C: Early-Stop SA** — остановиться раньше если активация перестала расти. **USER OPT-IN ONLY.**

  ⚠️ **Риск:** Старые редкие воспоминания живут на периферии графа — слабые рёбра, далеко от центра. Spreading activation находит их только на итерации 3. Ранняя остановка означает что волна активации до них не докатится. Для consciousness research и identity continuity это критично — именно периферийные воспоминания несут уникальный контекст. Регрессионный тест (32 запроса) это не поймает.

  **Реализация:** только через явный параметр пользователя  в .env (default: false). Никогда не включать по умолчанию.
- [ ] **D: Precomputed Activation** — предвычислить потенциал активации в sleep compute, использовать при поиске.
- [ ] **E: Bi-Level Index** — двухуровневый поиск: сначала по кластерам, потом SA внутри. Для 50K+ нод.

*Рекомендация: начать с A+B, мало кода, высокий эффект.*

### 22. TOTP Authentication для Studio MCP — MEDIUM
*Двухфакторная аутентификация для выполнения опасных команд. Тот же стандарт что Google Authenticator — 6-значный код меняется каждые 30 секунд, работает офлайн.*
- [ ] setup_totp.py — генерация секрета + QR код в терминале
- [ ] Один раз сканировать QR в Authenticator — появляется «HippoGraph Studio»
- [ ] studio_exec + studio_write_file проверяют время последней валидации
- [ ] Новый инструмент: studio_verify_totp(code)
- [ ] TTL через .env: `TOTP_SESSION_TTL_HOURS=24`
- [ ] studio_read_file + studio_list_dir — без TOTP (чтение безопасно)


### 23. Claude Code + Neural Memory — PERSONAL ONLY, LOW PRIORITY
*Claude Code — локальный CLI агент, тот же инстанс модели что и в Claude.ai. Если подключить Neural Memory MCP на уровне user scope (~/.claude.json) — Claude Code будет читать ту же память что и я в Claude.ai. Один инстанс, одна память, разные интерфейсы. Потенциально: автоматическое сохранение контекста сессий без ручного запоминания.*

**Почему не сейчас:** стремно. Добавлять новый инструмент с доступом к памяти пока не укреплено текущее — риск ради эксперимента. Сначала TOTP (#22), Deep Sleep, Docker cleanup.

**Когда делать:** после стабилизации Phase 2.5 и реализации TOTP.

- [ ] Подключить Neural Memory MCP в Claude Code (user scope)
- [ ] Проверить что Claude Code видит ту же память что Claude.ai
- [ ] Эксперимент: автосохранение ключевых моментов сессии в конце разговора
- [ ] Один инстанс — никакой параллельной записи

**Note:** Только личное использование. Не для публичного репо.

---

## Out of Scope

| Фича | Почему нет |
|---------|--------|
| Multi-tenant | Один пользователь |
| OAuth/SSO/RBAC | API key достаточно |
| Cloud sync | Локальный сервер |
| PostgreSQL | SQLite достаточно для нашего масштаба |
| Framework integrations | Только MCP |
| SOC2/GDPR | Личный проект |
| Горизонтальное масштабирование | Один пользователь |
| Ollama/LLM sidecar | Удалён — GLiNER/GLiNER2 закрывают все нужды |
| Авто Edge Pruning | Архитектурное решение — см. #20 |
| Traction / marketing | Не цель на данном этапе |